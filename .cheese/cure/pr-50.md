status: ok
next: done
artifact: .cheese/affinage/pr-50.md
Replaced the vacuous 4-byte 🦀 test char with a 3-byte ☃ so the newline-free truncation test actually exercises the #39 mid-codepoint panic.

## Cure Report

### Applied

- **[from-comment:3328012300] [assertions:medium]** `src/budget.rs` `apply_with_info_no_newline_at_all_does_not_panic`: switched the repeated char from `"🦀"` (4 bytes) to `"☃"` (U+2603, 3 bytes). `max_bytes = content_budget * 4` is always a multiple of 4, so an all-🦀 body stays char-aligned and the test passed even against the buggy `.unwrap_or(max_bytes)`. With ☃ at budget 100, `max_bytes = 200` falls mid-codepoint (boundary at 198), so the test now fails on pre-fix code and passes only because of the `.unwrap_or(safe_max)` clamp.

### Deferred

- (none)

### Checks

- `cargo test apply_with_info_no_newline`: pass
- `cargo test budget`: pass (11 tests)
- `cargo clippy -- -D warnings`: pass
- `cargo fmt --check`: pass
- Pre-fix regression proof: temporarily reverted `apply` to `.unwrap_or(max_bytes)` → test panics `end byte index 200 is not a char boundary; it is inside '☃'`; restored → green. The test now verifies intent, not just behavior (Rule 6).

### Re-review

- Remaining risk: none — single test-only edit, fully gated.
- Suggested next step: push to PR #50; the original #39 production fix is already on main.
