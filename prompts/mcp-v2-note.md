TRIAL: `tilth_search_v2` is advertised. DO NOT route find/explore to `tilth_search`/`tilth_grok`/`tilth_deps` when `tilth_search_v2` is available; call it instead: ordered `queries: [{query: "foo"}, {query: "bar", glob: "*.rs"}]` (1-10 items), `cwd` required. The server owns routing — no `kind`/`expand`/`context` — and returns JSON `{results, hints, diagnostics}`; follow `hints` for continuations.
