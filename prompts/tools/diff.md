Structural diff showing function-level changes. Replaces git diff. Call with no args for uncommitted changes overview.

DO NOT use `search` to filter by symbol name — it matches raw hunk text, not identifiers. Use `scope: "src/foo.go"` to filter to a file; use `search: "panic"` to filter hunks containing that text.
